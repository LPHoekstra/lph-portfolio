function Footer() {
    return (
        <footer>
            {/* Lien utile style github, linkedin... en mode icon */}
            {/* mon nom + version ? */}
            <p>by ...</p>
        </footer>
    )
}

export default Footer